APP_ENV=local
BUILD_VERSION=dev
HOST=0.0.0.0
PORT=5000
DATABASE_URL=postgresql://postgres:password@localhost:5432/goed_hackathon
DB_SSLMODE=require

# Navigator chat (OpenRouter). Required in production on the backend Vercel project.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct
LLM_TIMEOUT_SECONDS=30
